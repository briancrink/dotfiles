#! /usr/local/bin/bash

pushd ~;
dtf=dotfiles.zip;
zip -FS -rv $setup/$dtf .dotfiles;
pushd $setup;
zip -FS -rv $docs/$dtf $dtf;
unset dtf;
popd; popd;

# pushd ~
# dotzip=$mac/dotfiles.zip;
# zip -FS -rv $dotzip .dotfiles;
# cp $dotzip $icloud;
# unset df; 
# popd;
