#! /usr/local/bin/bash

pushd $HOME;
dotf=dotfiles.zip;
zip -FS -rv $ws/$dotf .dotfiles;
# pushd $ws; # # * iCloud Drive sync issues on macOS Catalina
# zip -FS -rv $docs/$dotf $dotf;
unset dotf;
# popd;
popd;
