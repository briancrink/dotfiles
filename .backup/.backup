# # * Folder .icns
fileicon get -f ~/Downloads ~/.dotfiles/.icns/downloads
fileicon get -f /Volumes/Drive/Workspace/Document/Scanner ~/.dotfiles/.icns/scanner
fileicon get -f /Volumes/Drive/Workspace/Document/Screenshot ~/.dotfiles/.icns/screenshot

# # * Brewfile (force overwrite)
brew bundle dump --force --file=~/.dotfiles/.brew/.Brewfile

# # * VS Code
# (backup sync-settings -> git gists)

# # * App data

# # * Finder View
# (com.apple.finder.plist -> convert to .. -> export to > ..)

# # * System Preferences
# (com.apple.***.plist -> ~convert to PlistBuddy bash commands~ -> ~export to~ -> ~/.sysprefs)

# # * Git commit / push

# # * Drive
# (workspace -> iCloud Drive -> Documents -> zipFile)

# # * ~prompt notification~
# (notication center -> backup has completed)
